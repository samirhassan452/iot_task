import 'package:flutter/material.dart';

class HomeAwayWidget extends StatelessWidget {
  const HomeAwayWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
